[package]
name = "hello-world-app"
version = "0.1.0"
edition = "2021"
include = ["**/.rs"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
hello-world-lib = { version = "$VERSION", registry = "private-repo" }
